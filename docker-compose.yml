services:
  api:
    build: .
    env_file:
      - .env.example
    environment:
      DATABASE_URL: sqlite:////data/falange.db
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./data:/data
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
